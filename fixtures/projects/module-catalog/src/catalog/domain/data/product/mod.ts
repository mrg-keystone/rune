// Scaffolded once; fill in the bodies. `sync` preserves this file.
import { ProductBase } from "./sig.ts";

export class Product extends ProductBase {
  override query(listDto: unknown): unknown {
    throw new Error("not implemented");
  }
}
