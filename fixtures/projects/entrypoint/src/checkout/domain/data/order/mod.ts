// Scaffolded once; fill in the bodies. `sync` preserves this file.
import { OrderBase } from "./sig.ts";

export class Order extends OrderBase {
  override save(placeOrderDto: unknown): unknown {
    throw new Error("not implemented");
  }
}
