// Lives in a "shared" grab-bag folder (loose-files) and is imported via a
// relative path above — both deliberate violations for the L4 fixture.
export function helper(x: unknown): unknown {
  return x;
}
