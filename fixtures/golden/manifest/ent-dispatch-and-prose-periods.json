{
  "module": "sample",
  "rune": "corpus/valid/ent-dispatch-and-prose-periods.rune",
  "toCreate": [
    {
      "path": "src/sample/domain/coordinators/fragment-ingest/int.test.ts",
      "content": "// Generated by rune manifest from corpus/valid/ent-dispatch-and-prose-periods.rune.\n// Edit the body. Re-running manifest will not overwrite this file.\n\n// Recipe (from [REQ] fragment.ingest @ corpus/valid/ent-dispatch-and-prose-periods.rune:3):\n//   1. [RET] AckDto\n\nimport { ingest } from \"./mod.ts\";\n\nDeno.test(\"ingest — happy path\", async () => {\n  // TODO: implement happy path test\n});\n\n\nDeno.test(\"write-failed\", async () => {\n  // TODO: assert this fault path\n});\n\n"
    },
    {
      "path": "src/sample/domain/coordinators/fragment-ingest/mod.ts",
      "content": "// Generated by rune manifest from corpus/valid/ent-dispatch-and-prose-periods.rune:3.\n// Edit the body. Re-running manifest will not overwrite this file.\n\nimport { FragmentDto } from \"@/src/sample/dto/fragment.ts\";\nimport { AckDto } from \"@/src/sample/dto/ack.ts\";\nimport { assert } from \"#assert\";\nimport { Fragment as FragmentData } from \"@/src/sample/domain/data/fragment/mod.ts\";\n\n/**\n * Coordinator for [REQ] fragment.ingest(FragmentDto): AckDto.\n * @param input FragmentDto — asserted against its contract at the seam.\n * @returns AckDto — asserted before return.\n * @throws write-failed — raised by fragment.save\n */\nexport async function ingest(input: FragmentDto): Promise<AckDto> {\n  const validInput = assert(FragmentDto, input, \"fragment.ingest input\");\n  const fragmentData = new FragmentData();\n\n  // core — pure business logic, no I/O\n  ingestCore(validInput);\n\n  // after the core — boundary calls in spec order (validated at the seam)\n  // (guards run in the core above: a throw there prevents every call below)\n  const fragmentSave = assert(AckDto, await fragmentData.save(validInput), \"fragment.save\");\n\n  return fragmentSave;\n}\n\n// Pure business logic for fragment.ingest — no I/O. Takes the\n// request input; returns nothing (the post-core boundary calls produce the flow's values).\nfunction ingestCore(input: FragmentDto): void {\n  // Recipe from [REQ] fragment.ingest (run in order):\n  //   1. [RET] AckDto\n  // TODO: implement the steps above, then build the dtos\n  throw new Error(\"not implemented\");\n}\n"
    },
    {
      "path": "src/sample/domain/data/fragment/mod.ts",
      "content": "// Generated by rune manifest from corpus/valid/ent-dispatch-and-prose-periods.rune.\n// Scaffolded once; fill in the bodies. `sync` preserves this file.\n\nimport { AckDto } from \"@/src/sample/dto/ack.ts\";\nimport { FragmentDto } from \"@/src/sample/dto/fragment.ts\";\n\n// Data adapter for `fragment` — the I/O boundary.\n// Coordinators await these methods; implement each one.\n// a profiler trace fragment from one tier (e.g. web)\nexport class Fragment {\n  /**\n   * @param fragmentDto one tier's fragment, posted to the collector (e.g. web, backend)\n   * @returns acknowledgement that the fragment was stored\n   * service: db (transport SIDECAR) — env: DB_URL\n   * the datastore, e.g. a local sqlite file\n   * @see https://docs.example.com/db\n   * @throws write-failed\n   */\n  save(fragmentDto: FragmentDto): Promise<AckDto> {\n    throw new Error(\"not implemented\");\n  }\n}\n"
    },
    {
      "path": "src/sample/domain/data/fragment/smk.test.ts",
      "content": "// Generated by rune manifest from corpus/valid/ent-dispatch-and-prose-periods.rune.\n// Edit the body. Re-running manifest will not overwrite this file.\n\n// Boundary methods to reach:\n//   Fragment.save(FragmentDto) (service: db)\n\nDeno.test(\"fragment — connectivity\", () => {\n  // TODO: smoke test that verifies the boundary is reachable\n});\n\n\nDeno.test(\"write-failed\", async () => {\n  // TODO: assert this fault path\n});\n\n"
    },
    {
      "path": "src/sample/dto/ack.ts",
      "content": "// Generated by rune manifest from corpus/valid/ent-dispatch-and-prose-periods.rune.\n// Edit the body. Re-running manifest will not overwrite this file.\n\nimport { IsBoolean } from \"class-validator\";\nimport { ApiProperty } from \"#api-doc\";\n\n/**\n * acknowledgement that the fragment was stored\n * @internal\n */\n// rune declares: [DTO] AckDto: ok\nexport class AckDto {\n  /** whether the ingest was accepted */\n  // rune declares: [TYP] ok: boolean\n  @ApiProperty({ description: \"whether the ingest was accepted\" })\n  @IsBoolean()\n  ok!: boolean;\n}\n"
    },
    {
      "path": "src/sample/dto/fragment.ts",
      "content": "// Generated by rune manifest from corpus/valid/ent-dispatch-and-prose-periods.rune.\n// Edit the body. Re-running manifest will not overwrite this file.\n\nimport { IsString } from \"class-validator\";\nimport { ApiProperty } from \"#api-doc\";\n\n/**\n * one tier's fragment, posted to the collector (e.g. web, backend)\n * @internal\n */\n// rune declares: [DTO] FragmentDto: service\nexport class FragmentDto {\n  /** the tier that produced the span (e.g. web, backend) */\n  // rune declares: [TYP:example=backend] service: string\n  @ApiProperty({ description: \"the tier that produced the span (e.g. web, backend)\", example: \"backend\" })\n  @IsString()\n  service!: string;\n}\n"
    },
    {
      "path": "src/sample/dto/ok.ts",
      "content": "// Generated by rune manifest from corpus/valid/ent-dispatch-and-prose-periods.rune:18.\n// Edit the body. Re-running manifest will not overwrite this file.\n\n/** whether the ingest was accepted */\n// rune declares: [TYP] ok: boolean\nexport type Ok = boolean;\n"
    },
    {
      "path": "src/sample/dto/service.ts",
      "content": "// Generated by rune manifest from corpus/valid/ent-dispatch-and-prose-periods.rune:16.\n// Edit the body. Re-running manifest will not overwrite this file.\n\n/** the tier that produced the span (e.g. web, backend) */\n// rune declares: [TYP:example=backend] service: string\nexport type Service = string;\n"
    },
    {
      "path": "src/sample/entrypoints/http/e2e.test.ts",
      "content": "// Generated by rune manifest from corpus/valid/ent-dispatch-and-prose-periods.rune.\n// Edit the body. Re-running manifest will not overwrite this file.\n\nimport { httpModule } from \"./mod.ts\";\nimport { bootstrapServer, exerciseEndpoints } from \"@mrg-keystone/rune\";\nimport { assertEquals } from \"#std/assert\";\n\n// Fill the coordinator bodies, then run with RUNE_E2E=1 to drive every endpoint\n// to green (orders by @Endpoint order, chains outputs into inputs via bind).\nDeno.test({\n  name: \"sample/http — endpoints run and chain\",\n  ignore: !Deno.env.get(\"RUNE_E2E\"),\n  fn: async () => {\n    const api = await bootstrapServer(\"sample\", httpModule, { swagger: true });\n    try {\n      // Endpoint chain (by @Endpoint order):\n      //   1. ingest -> AckDto\n      const report = await exerciseEndpoints({ api });\n      assertEquals(report.failed.map((r) => r.id), []);\n    } finally {\n      await api.stop();\n    }\n  },\n});\n"
    },
    {
      "path": "src/sample/entrypoints/http/mod.ts",
      "content": "// Generated by rune manifest from corpus/valid/ent-dispatch-and-prose-periods.rune.\n// Edit the body. Re-running manifest will not overwrite this file.\n\nimport { Endpoint, EndpointController, endpointModule } from \"@mrg-keystone/rune\";\nimport { FragmentDto } from \"@/src/sample/dto/fragment.ts\";\nimport { AckDto } from \"@/src/sample/dto/ack.ts\";\nimport { ingest as fragmentIngest } from \"@/src/sample/domain/coordinators/fragment-ingest/mod.ts\";\n\n@EndpointController(\"http\")\nexport class HttpController {\n  /**\n   * Delegates to coordinator fragment.ingest (named by the [ENT] body).\n   * @param body FragmentDto — one tier's fragment, posted to the collector (e.g. web, backend)\n   * @returns AckDto — acknowledgement that the fragment was stored\n   * @throws write-failed (fragment.save)\n   */\n  // from corpus/valid/ent-dispatch-and-prose-periods.rune:8\n  @Endpoint({ path: \"ingest\", input: FragmentDto, output: AckDto, order: 1 })\n  ingest(body: FragmentDto): Promise<AckDto> {\n    return fragmentIngest(body);\n  }\n}\n\nexport const httpModule = endpointModule(\"Sample\", [HttpController]);\n"
    }
  ],
  "toRegenerate": [
    {
      "path": "src/sample/mod-root.ts",
      "content": "// Generated by rune manifest — DO NOT EDIT (regenerated on every `rune sync`).\n\n// Domain nouns (from [NON]):\n//   fragment: a profiler trace fragment from one tier (e.g. web)\n//\n// Type vocabulary (from [TYP]):\n//   service: string — the tier that produced the span (e.g. web, backend)\n//   ok: boolean — whether the ingest was accepted\n//\n// Backing services (shared, from src/core/core.rune):\n//   db (SIDECAR): DB_URL\n// Public API surface for module \"sample\".\n\nexport { ingest } from \"./domain/coordinators/fragment-ingest/mod.ts\";\n\n"
    }
  ],
  "toSkip": [],
  "errors": []
}
